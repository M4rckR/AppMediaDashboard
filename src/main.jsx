import React from 'react'
import ReactDOM from 'react-dom/client'
import AppMediaDashboard from './AppMediaDashboard'

import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppMediaDashboard />
  </React.StrictMode>,
)
